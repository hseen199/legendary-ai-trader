# 📦 دليل التثبيت الشامل - نظام التداول الخارق V3.0
# Complete Installation Guide - Legendary Trading System V3.0

---

## 🖥️ متطلبات النظام

### الحد الأدنى
- **نظام التشغيل**: Windows 10/11, Ubuntu 20.04+, macOS 11+
- **Python**: 3.10 أو 3.11 (موصى به 3.11)
- **RAM**: 8 جيجا على الأقل (16 جيجا موصى به)
- **مساحة القرص**: 5 جيجا على الأقل
- **اتصال إنترنت**: مطلوب

---

## 📋 خطوات التثبيت

### الخطوة 1: تثبيت Python 3.11

#### Windows:
```bash
# حمّل من الموقع الرسمي
https://www.python.org/downloads/release/python-3110/

# أو باستخدام winget
winget install Python.Python.3.11
```

#### Ubuntu/Debian:
```bash
sudo apt update
sudo apt install python3.11 python3.11-venv python3.11-dev python3-pip
```

#### macOS:
```bash
brew install python@3.11
```

---

### الخطوة 2: فك ضغط المشروع

```bash
# فك الضغط
tar -xzvf legendary_trading_system_v3_complete.tar.gz

# الدخول للمجلد
cd legendary_merged
```

---

### الخطوة 3: إنشاء البيئة الافتراضية

```bash
# إنشاء البيئة
python3.11 -m venv venv

# تفعيل البيئة
# Linux/macOS:
source venv/bin/activate

# Windows:
venv\Scripts\activate
```

---

### الخطوة 4: تحديث pip

```bash
pip install --upgrade pip setuptools wheel
```

---

### الخطوة 5: تثبيت المتطلبات الأساسية

```bash
# تثبيت جميع المتطلبات
pip install -r requirements.txt
```

**أو تثبيت يدوي بالترتيب:**

```bash
# 1. المكتبات الأساسية
pip install numpy pandas scipy

# 2. التعلم العميق (اختر واحد حسب جهازك)
# للأجهزة مع GPU NVIDIA:
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# للأجهزة بدون GPU:
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# 3. التعلم المعزز
pip install gymnasium stable-baselines3

# 4. تحسين المعاملات
pip install optuna

# 5. التحليل الفني
pip install ta pandas-ta

# 6. Binance API
pip install python-binance ccxt

# 7. LLM
pip install openai tiktoken

# 8. تحليل المشاعر
pip install transformers vaderSentiment textblob

# 9. قواعد البيانات
pip install sqlalchemy aiosqlite redis

# 10. الشبكات
pip install aiohttp httpx websockets

# 11. الأدوات المساعدة
pip install pyyaml python-dotenv loguru rich tqdm tenacity
```

---

### الخطوة 6: تثبيت TA-Lib (اختياري لكن موصى به)

#### Ubuntu/Debian:
```bash
# تثبيت المكتبة C أولاً
sudo apt-get install build-essential
wget http://prdownloads.sourceforge.net/ta-lib/ta-lib-0.4.0-src.tar.gz
tar -xzf ta-lib-0.4.0-src.tar.gz
cd ta-lib/
./configure --prefix=/usr
make
sudo make install
cd ..

# ثم تثبيت Python wrapper
pip install ta-lib
```

#### Windows:
```bash
# حمّل الملف المناسب من:
# https://www.lfd.uci.edu/~gohlke/pythonlibs/#ta-lib

# مثال لـ Python 3.11 64-bit:
pip install TA_Lib-0.4.24-cp311-cp311-win_amd64.whl
```

#### macOS:
```bash
brew install ta-lib
pip install ta-lib
```

---

### الخطوة 7: إعداد ملف البيئة (.env)

```bash
# نسخ الملف النموذجي
cp .env.example .env

# تعديل الملف
nano .env  # أو أي محرر نصوص
```

**محتوى ملف .env:**
```env
# ═══════════════════════════════════════════════════════════════
#                    إعدادات نظام التداول الخارق
# ═══════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────
# Binance API (مطلوب)
# ─────────────────────────────────────────────────────────────────
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_api_secret_here

# ─────────────────────────────────────────────────────────────────
# OpenAI API (اختياري - للتحليل الذكي)
# ─────────────────────────────────────────────────────────────────
OPENAI_API_KEY=your_openai_api_key_here

# ─────────────────────────────────────────────────────────────────
# إعدادات إضافية (اختياري)
# ─────────────────────────────────────────────────────────────────
# Alpha Vantage (للبيانات المالية)
ALPHA_VANTAGE_API_KEY=

# Glassnode (لبيانات On-Chain)
GLASSNODE_API_KEY=

# CryptoCompare
CRYPTOCOMPARE_API_KEY=

# ─────────────────────────────────────────────────────────────────
# إعدادات النظام
# ─────────────────────────────────────────────────────────────────
# وضع التشغيل: live أو paper
TRADING_MODE=live

# مستوى التسجيل: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# المنطقة الزمنية
TIMEZONE=UTC
```

---

### الخطوة 8: التحقق من التثبيت

```bash
# تشغيل اختبار سريع
python -c "
import torch
import numpy as np
import pandas as pd
from binance.client import Client
import openai

print('✅ PyTorch:', torch.__version__)
print('✅ NumPy:', np.__version__)
print('✅ Pandas:', pd.__version__)
print('✅ جميع المكتبات مثبتة بنجاح!')
"
```

---

### الخطوة 9: التشغيل

```bash
# التشغيل المتكامل
python main_integrated.py

# أو التشغيل الأصلي
python main.py
```

---

## 📦 قائمة المتطلبات الكاملة (requirements.txt)

```
# ═══════════════════════════════════════════════════════════════
#                    المتطلبات الكاملة
# ═══════════════════════════════════════════════════════════════

# المكتبات الأساسية
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# التعلم العميق
torch>=2.0.0
torchvision>=0.15.0

# التعلم المعزز
gymnasium>=0.28.0
stable-baselines3>=2.0.0

# تحسين المعاملات
optuna>=3.0.0

# التحليل الفني
ta>=0.10.0
pandas-ta>=0.3.0
# ta-lib>=0.4.0  # يحتاج تثبيت يدوي

# Binance API
python-binance>=1.0.0
ccxt>=4.0.0

# LLM
openai>=1.0.0
tiktoken>=0.5.0

# تحليل المشاعر والأخبار
transformers>=4.30.0
vaderSentiment>=3.3.0
textblob>=0.17.0

# قواعد البيانات
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
redis>=4.5.0

# الشبكات والـ Async
aiohttp>=3.8.0
httpx>=0.24.0
websockets>=11.0.0

# التخزين
pyarrow>=12.0.0
h5py>=3.8.0
joblib>=1.3.0

# الإعدادات والتسجيل
pyyaml>=6.0.0
python-dotenv>=1.0.0
loguru>=0.7.0
rich>=13.0.0

# التصور
matplotlib>=3.7.0
plotly>=5.15.0
seaborn>=0.12.0

# أدوات مساعدة
tqdm>=4.65.0
python-dateutil>=2.8.0
tenacity>=8.2.0
cachetools>=5.3.0

# scikit-learn
scikit-learn>=1.2.0
xgboost>=1.7.0
lightgbm>=3.3.0
```

---

## 🔧 حل المشاكل الشائعة

### مشكلة: خطأ في تثبيت torch
```bash
# جرب التثبيت بدون GPU
pip install torch --index-url https://download.pytorch.org/whl/cpu
```

### مشكلة: خطأ في ta-lib
```bash
# استخدم pandas-ta كبديل
pip install pandas-ta
```

### مشكلة: خطأ في الذاكرة
```bash
# قلل حجم الدفعة في config.yaml
batch_size: 32  # بدلاً من 64
```

### مشكلة: خطأ في Binance API
```bash
# تأكد من:
# 1. المفاتيح صحيحة
# 2. الصلاحيات مفعلة (Spot Trading)
# 3. IP مسموح به
```

---

## 📞 للمساعدة

إذا واجهت أي مشكلة:
1. تأكد من اتباع الخطوات بالترتيب
2. تحقق من رسائل الخطأ
3. ابحث عن الخطأ في Google
4. أرسل رسالة الخطأ كاملة

---

**تم إعداد هذا الدليل بتاريخ**: 5 يناير 2026
